using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AdvancedEnemyController : MonoBehaviour
{
    ///Make a enemy that walks like a humanoid that has hearing(optional) and has a retreat function,reload function(optional),
    ///     move forward, and patrol
    ///     when health is below a certain level make them retreat behind a shield of their own 
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
